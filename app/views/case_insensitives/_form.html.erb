<%= form_for(@case_insensitive) do |f| %>
  <% if @case_insensitive.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@case_insensitive.errors.count, "error") %> prohibited this case_insensitive from being saved:</h2>

      <ul>
      <% @case_insensitive.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
